# Backend origin. Leave empty in dev: requests go same-origin through the Vite proxy.
# Set to e.g. https://api.codeaudit.example in deployed builds.
VITE_API_ORIGIN=
# Where the Vite dev server proxies /api and /health.
VITE_API_PROXY_TARGET=http://localhost:8000
